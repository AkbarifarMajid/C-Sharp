<Window x:Class="InvoiceApp.Views.AddCustomerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ADD New Customer" Height="250" Width="400"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--  Name Customer -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Customer Name:" Width="100" VerticalAlignment="Center"/>
            <TextBox Width="200" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <!-- ID Customer  -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Customer NR:" Width="100" VerticalAlignment="Center"/>
            <TextBox Width="200" Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <!--  Adress Customer  -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Address:" Width="100" VerticalAlignment="Center"/>
            <TextBox Width="200" Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <!--Bottons -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Right">
            <Button Content="Save" Width="80" Margin="5" Command="{Binding SaveCommand}"/>
            <Button Content="Cancel" Width="80" Margin="5" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
